{
  "name": "refred",
  "version": "0.1.0",
  "private": true,
  "description": "Referrer-Relative URL Redirection",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.0.1",
    "@cloudflare/workers-types": "^4.20230518.0",
    "typescript": "^5.1.3",
    "vitest": "^0.32.2",
    "wrangler": "^3.1.1"
  }
}
